[variables]
NIXPACKS_NODE_VERSION = "22"
NIXPACKS_PHP_ROOT_DIR = "/app/public"

[phases.setup]
aptPkgs = ["...", "supervisor"]

[phases.build]
cmds = ["...", "chmod +x /app/docker/production/start.sh"]

[start]
cmd = "/app/docker/production/start.sh"
